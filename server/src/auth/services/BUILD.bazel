package(default_visibility=["//visibility:public"])
load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = "services",
    srcs = glob(["*.ts"]),
    deps = [
        "//src/auth/services/auth:authservice",
    ],
)