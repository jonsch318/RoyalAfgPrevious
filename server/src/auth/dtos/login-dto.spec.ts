import { LoginDto } from './login-dto';

/**
 * Automatically generated test for the login dto class.
 */
describe('LoginDto', () => {
  it('should be defined', () => {
    expect(new LoginDto()).toBeDefined();
  });
});
