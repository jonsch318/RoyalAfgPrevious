import { RegisterDto } from './register-dto';

/**
 * Automatically generated tests for the register dto
 */
describe('RegisterDto', () => {
  it('should be defined', () => {
    expect(new RegisterDto()).toBeDefined();
  });
});
