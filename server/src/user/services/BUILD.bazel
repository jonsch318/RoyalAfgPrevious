mongoosepackage(default_visibility=["//visibility:public"])
load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = "authcontroller",
    srcs = glob(["*.ts"]),
    deps = [
        "//src/user/services/user:userservice",
    ],
)