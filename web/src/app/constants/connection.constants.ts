// Provides configuration for the connection to the server
// The default path to the server
export const APIURL = "http://localhost:3000/api"; 

// whether cors is enabled or not
export const CORSENABLED = true;
